
{% extends 'base.html.twig' %}

{% block title %}Hello BlogController!{% endblock %}

{% block body %}
    <ul>
    {% for title in titles %}
        <li> {{ title }} </li>
        {% endfor %}
        </ul>

  <h1> {{ message }} </h1>

    <h1>Mes articles</h1>

    {% for article in articles %}
        <h2>{{ article.title }}</h2>
    {% endfor %}

{% endblock %}
