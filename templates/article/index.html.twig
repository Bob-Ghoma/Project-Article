{% extends 'base.html.twig' %}

{% block title %}Hello BlogController!{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Articles</h1>
        <ul>
            {% for article in articles %}
                <li> <a href="{{ path('articles_read', {"article":article.id}) }}"></a> {{ article.title}} </li>
            {% endfor %}
        </ul>

        <a href="{{ path('articles_create') }}"> Crée un article</a>
    </div>

{% endblock %}
